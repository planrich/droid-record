Android Record
===


Changelog
===

* v0.1.4 -
* v0.1.0 - 11.Oct.2013
    * cache findBy()
    * query interface, limit(0,1), limit(1), distinct()
    * relations has_one
    * doc show what json generates
      json {   |  record.can()
              --> record.do()
      }        |  record.that()
    * create table, null, unique and constraint
    * gradle -> migrate on file changes
    * bugfix intellij start removes some files? wtf?

